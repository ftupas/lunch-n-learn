CREATE TABLE covid (
    country VARCHAR(255),
    total_cases INT,
    total_deaths INT,
    total_recovered INT,
    active_cases INT,
    total_cases_per_1m_pop INT,
    total_tests INT,
    deaths_per_1m_pop INT,
    tests_per_1m_pop INT,
    population INT,
    death_rate FLOAT
)

